import Image from 'next/image'
import { ArrowUpRight } from 'lucide-react'
import type { ApplicationSummary } from '@/features/catalog/types'
import type { Locale } from '@/i18n/locales'
export function ApplicationCard({ application, locale, featured = false }: { application: ApplicationSummary; locale: Locale; featured?: boolean }) { const isSurf = application.slug === 'surf-watersports'; return <article className={`application-card ${featured ? 'application-card--featured' : ''}`}><div className={`application-card__media application-card__media--${application.tone}`}>{isSurf ? <Image alt="AI draft of a rubber patch on watersports gear" fill sizes="(max-width: 800px) 100vw, 50vw" src="/media/drafts/surf-watersports-gear-ai-draft.png"/> : <span aria-hidden="true">{application.index}</span>}<small>{isSurf ? 'AI DRAFT VISUAL' : 'APPLICATION'}</small></div><div className="application-card__body"><span>{application.index}</span><h3><a href={`/${locale}/applications/${application.slug}`}>{application.name}<ArrowUpRight aria-hidden="true" size={18}/></a></h3><p>{application.description}</p></div></article> }
